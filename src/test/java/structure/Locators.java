package structure;

public class Locators {
    //Локатор кнопки заказать наверху страницы
    //button[@class='Button_Button__ra12g']

    // Локатор кнопки заказать внизу страницы
    //button[@class='Button_Button__ra12g Button_Middle__1CSJM']

    //Локатор вопроса
    //div[contains(text(), '" + questionText + "')]"

    //Локатор ответа
    //p[contains(text(), '" + expectedAnswerText + "')]
}
